{
  "name": "@butler-tech/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch index.js",
    "start": "node --env-file-if-exists=.env index.js",
    "test": "node --test",
    "check": "biome check .",
    "fix": "biome check --write ."
  },
  "imports": {
    "#/*": "./src/*"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:"
  },
  "dependencies": {
    "evlog": "^2.28.1",
    "express": "^5.2.1",
    "valibot": "^1.4.2"
  }
}
